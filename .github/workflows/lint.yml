name: Lint

on:
  push:
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

env:
  PYTHON_VERSION: 3.14

jobs:
  lint:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v7

      - name: Set up uv
        uses: astral-sh/setup-uv@v9.0.0
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          enable-cache: true

      - name: Install dependencies
        run: uv sync --locked --all-extras --all-groups

      - name: Run Ruff check
        id: ruff_check
        run: uv run ruff check vardefunc

      - name: Run Ruff format
        id: ruff_format
        if: success() || (failure() && steps.ruff_check.conclusion == 'failure')
        run: uv run ruff format --check --diff

      - name: Running mypy
        if: success() || (failure() && (steps.ruff_check.conclusion == 'failure' || steps.ruff_format.conclusion == 'failure'))
        run: uv run mypy vardefunc
